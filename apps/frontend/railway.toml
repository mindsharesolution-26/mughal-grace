[build]
builder = "nixpacks"
buildCommand = "pnpm install && pnpm --filter @mughal-grace/frontend build"

[deploy]
startCommand = "pnpm --filter @mughal-grace/frontend start"
healthcheckPath = "/"
healthcheckTimeout = 100
restartPolicyType = "on_failure"
restartPolicyMaxRetries = 3
